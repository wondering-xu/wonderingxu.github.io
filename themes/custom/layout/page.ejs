<section class="page">
  <% if (page.title) { %>
  <header class="page-header">
    <h1 class="page-title"><%= page.title %></h1>
  </header>
  <% } %>
  <div class="page-body">
    <%- page.content %>
  </div>
</section>
