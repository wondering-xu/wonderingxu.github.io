<!doctype html>
<html lang="<%= (page.lang || config.language || 'en').toString().split('_')[0] %>">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= page.title ? page.title + ' · ' + (config.title || 'Hexo') : (config.title || 'Hexo') %></title>
  <% if (page.description || config.description) { %>
  <meta name="description" content="<%= page.description || config.description %>">
  <% } %>
  <link rel="stylesheet" href="<%= url_for('/css/styles.css?v=theme-1') %>">
</head>
<body>
  <%- partial('partial/navbar', { page }) %>
  <main>
    <%- body %>
  </main>
  <%- partial('partial/footer', { page }) %>
</body>
</html>
